# Jarvis: 비서

Jarvis는 Windows 환경에서 실행하는 데스크톱 전용 '비서' AI 도우미입니다.

## 📌 1. 프로젝트 개요 (Introduction)

**Jarvis**는 회사 내의 복잡한 문서(PDF) 규정부터 사용자의 로컬 컴퓨터 수백만 개의 파일, 그리고 외부 웹 실시간 정보까지 하나로 통합하여 대답해 주는 AI 어시스턴트입니다.

### 주요 기능 요약
- 🤖 **Ollama 기반 로컬 LLM 채팅**
- 📚 **사내 PDF 문서 RAG 검색**
- 🔍 **Everything 기반 초고속 PC 파일 검색**
- ⚡ **네이버 실시간 다이렉트 스크래핑 (`bs4` 활용):** 날씨, 주가 등 실시간 위젯 정보를 정확하게 수집하여 환각 현상 없이 데이터 기반으로 응답
- 📈 **주식 기술적 분석 (`yfinance` 활용):** 삼성전자, 네이버, 엔비디아 등 30여 개 종목의 실시간 OHLCV 데이터를 바탕으로 RSI·MACD·볼린저밴드를 정밀 계산하여 제공
- 🖥️ **와이드 채팅 UI & 고속 타이핑 엔진:** 가독성을 극대화한 넓은 채팅창 인터페이스와 비동기 백그라운드 렌더링을 통한 부드러운 타이핑 효과 제공
- 🛡️ **고효율 마스터 페르소나:** 불필요한 서론/결론을 배제하고, 마스터의 요청에 핵심 정보와 결론부터 즉시 응답하는 고효율 비서 스타일 적용
- 💾 **채팅 이력 로컬 안전 저장 및 PyInstaller 1클릭 빌드**

### 🛠️ 핵심 기술 스택 (Tech Stack)
소스코드를 공부하거나 기능 확장을 기획 중인 개발자를 위한 기술 요약입니다.

**🟢 Core & GUI**
- **Python (v3.12.2) / PyQt5**: 마스터 컨트롤러 백엔드와 데스크톱 창(Window) 인터페이스 구축
- **QThreadPool & QRunnable**: 연산 부하가 큰 마크다운 렌더링을 백그라운드에서 처리하여 저사양 PC에서도 끊김 없는 UI 반응성 확보

**🔴 AI & NLP (로컬 LLM 및 RAG)**
- **Ollama (기본 모델: `llama3.1:8b`)**: 인터넷 연결 없이 완벽한 보안 환경에서 작동하는 로컬 언어 모델 코어
- **ChromaDB**: PDF 문서 기반 질문(RAG)을 처리하기 위한 로컬 벡터 데이터베이스 (Vector DB)
- **SentenceTransformers**: 로컬 텍스트의 문맥을 계산해서 벡터 값으로 변환해주는 경량화 AI

**🔵 Data & Search Pipeline (정보 탐색부)**
- **Everything (voidtools)**: 윈도우 OS의 레지스트리를 읽어 1초 만에 PC 파일과 폴더를 모조리 찾아내는 극한의 탐색 엔진
- **BeautifulSoup4 (bs4) & lxml**: 네이버 등 국내 검색 엔진 0순위 실시간 파싱(스크래핑) 모듈 (최신 파편화 위젯 데이터 즉시 수집)
- **yfinance**: Yahoo Finance API를 랩핑하여 코스피/코스닥 야후파이낸스에서 OHLCV(당일 산정가 데이터)를 실시간 수집
- **Tavily Search API**: 정교한 기술 및 최신 웹 문맥을 수집하는 타겟팅 웹 검색 API 파이프라인
- **DuckDuckGo (DDG)**: 환율/단위 등 보조 검색을 커버하는 후순위 지역화 검색 모듈

**🟡 Deployment (배포)**
- **PyInstaller**: 모든 라이브러리와 런타임 모델 캐시를 포함한 단일 실행 파일(.exe)로 패키징

---

## 🚀 2. 빠른 시작 (Quick Start)

### 🥇 [일반 사용자용] 1분 만에 실행하기
개발 환경을 모르는 **일반 사용자**라면 빌드된 `.exe` 파일만 클릭하면 됩니다!

1. **Ollama 사전 설치**: LLM(인공지능) 뇌 역할을 하는 코어 엔진을 다운로드합니다.
   - 다운로드: `https://ollama.com/download`
   - 기본 모델 설치: 터미널에서 `ollama pull llama3.1:8b` 실행
2. **Jarvis 실행**: 배포받은 폴더 안에서 `dist\jarvis\jarvis.exe` 를 더블클릭합니다.

### 🥈 [개발자용] 소스 기반 실행하기
코드를 수정하거나 튜닝하려는 **개발자** 전용 명령어입니다.

```powershell
# 1. 가상환경 생성 및 필수 패키지 설치
python -m venv venv
venv\Scripts\pip install -r requirements.txt

# 2. (선택사항) 환경설정 파일 복제
copy .env.example .env

# 3. 개발용 런타임 바로 가동
venv\Scripts\python.exe jarvis.py
```

### 🚨 [중요] 가동 전 필수 체크리스트 (Pre-run Checklist)
Jarvis가 정상적으로 대답하지 못한다면 다음을 가장 먼저 확인하세요!
- [ ] **Ollama 실행 여부**: PC 하단 트레이 아이콘에 Ollama가 켜져 있는지 확인
- [ ] **모델 설치 여부**: 터미널에서 `ollama pull llama3.1:8b` (또는 `.env` 지정 모델) 완료 상태인지 확인
- [ ] **환경변수(.env) 파일**: 루트 폴더에 `.env` 파일이 생성되어 있는지 (없으면 `.env.example` 복사 후 생성)
- [ ] **네트워크 상태**: 외부 웹 검색 기능을 사용하려면 인터넷 연결 및 API Key 입력 상태 확인

### 자주 쓰는 명령어 요약 

| 목적             | 명령어                                                                     |
| ---------------- | -------------------------------------------------------------------------- |
| **앱 실행**          | `venv\Scripts\python.exe jarvis.py`                                        |
| **벡터 DB 색인**   | `venv\Scripts\python.exe src\rag\ingest.py`                                |
| **워커 백엔드 테스트**  | `venv\Scripts\python.exe src\rag\rag_query_worker.py "휴가 규정 알려줘" 1` |
| **실행파일(exe) 빌드**        | `scripts\build_jarvis.bat`                                                 |

---

## 👤 3. 일반 사용자 가이드 (User Guide)

### Jarvis로 할 수 있는 주요 동작
- **웹 실시간 검색**: "세종 일기예보 어때", "카카오 주가 알려줘" 등 동적 위젯 데이터가 필요한 질문 시, 네이버를 다이렉트로 스크래핑하여 절대적으로 정확한 정보를 가져옵니다.
- **주식 기술적 분석** (삼성전자, 카카오, 네이버, 엔비디아 등 30여 개 지원):
  > "`삼성전자 기술적 분석해줘`", "`삼성전자 RSI 알려줘`", "`삼성전자 예상해줘`", "`삼성전자 예측해줘`" 등으로 질문
  - RSI(14) · MACD(12,26,9) · 볼린저밴드(20, 2σ) · MA5/20/60 · 거래량을 실제 데이터로 수치 계산
  - 산출된 수치를 기반으로 LLM이 한국어로 종합 해석 구성 (서론/결론 없이 지표 중심 요약)
  - ⚠️ 투자 조언은 제공하지 않으며, 실제 데이터 기반의 기술적 해석만 제공
- **내부 문서 질문**: "회사 연차 규정 알려줘" 와 같은 사내망 질문 시, `data\vectordb`를 교차 탐색하여 정확한 규칙을 끌어냅니다.
- **PC 파일 검색**: "컴퓨터에서 2024년 기획서 파일 찾아줘" 등 검색 시, 내장된 Everything 엔진을 가동해 윈도우 로컬의 모든 파일을 찾아 UI 리스트 형태로 제공합니다.
- **고효율 대화 및 코딩 멘토링**: 모든 응답은 마스터 페르소나를 기반으로 하며, 인사나 자기소개 등 불필요한 수사를 생략하고 마스터에게 필요한 정답만을 즉시 생성합니다.

### 사용자 주의사항
- `Tavily` 등의 외부 검색 API Key가 지정되지 않으면 일반 검색 엔진은 비활성화됩니다. 단, **네이버 직접 스크래핑 기능이나 내부/파일 검색은 여전히 작동합니다.**
- Ollama 서버가 켜져 있지 않거나 사용할 모델(`OLLAMA_MODEL_NAME`)이 설치되어 있지 않으면 어시스턴트는 대답하지 못합니다.
- 초기 내부 문서 검색 구동을 위해서는 `data\model_cache`가 준비되어 있어야 하지만 기본 배포판은 이를 모두 포함하고 있습니다.
- PC 파일 검색은 윈도우 한경에서 구동되며 Everything 권한이 필요합니다.

---

## 💻 4. 개발자 가이드 (Developer Guide)

이 섹션은 소스코드를 직접 수정하고 운영하는 유지보수 개발자를 위한 영역입니다.

### 환경 변수(.env)와 민감 정보 파이프라인
프로젝트 루트의 `.env` 파일이 모든 기능을 중앙에서 통제합니다. GitHub 같은 퍼블릭 저장소에는 절대 올라가지 않습니다.

- `.env.example`은 Git에 포함되며, 뼈대 구조를 알려줍니다.
- 이 구조를 참고해 개인 PC에 `.env`를 만들어 사용합니다.

```env
TAVILY_API_KEY=여기에_실제_키_입력
OLLAMA_MODEL_NAME=llama3.1:8b
EVERYTHING_PORT=8888
RAG_COLLECTION_NAME=jarvis_docs
RAG_EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2
```

### 저장소 아키텍처 및 폴더 구조
```text
Root/
├── src/                  # 메인 소스 코드 구역
│   ├── app/              # 메인 윈도우 및 LLM 챗 워커 로직
│   ├── core/             # 라우팅 매니저, 경로 관리 등 코어 공통 코드
│   ├── services/         # Everything 파일 서치 훅, 네이버/Tavily 웹 스크래핑 모듈
│   ├── repositories/     # 채팅 로컬 JSON 저장소 계층
│   ├── ui/               # 말풍선(Bubbles), 마크다운 렌더러 등 PyQt5 디자인 위젯
│   └── rag/              # 임베딩/인덱싱 및 RAG 워커 전용 스크립트 모음
├── data/                 # 런타임 데이터 허브 (Git ignore 대상)
│   ├── documents/        # RAG용 오리지널 사내 PDF를 보관하는 장소
│   ├── vectordb/         # PDF 청크(Chunk)가 저장되는 Chroma DB 폴더
│   ├── model_cache/      # 인터넷 오프라인 지원용 SentenceTransformer 모델 캐시
│   └── chats/            # 사용자 채팅 히스토리
├── assets/               # 앱 아이콘, Everything 엔진 실행 파일 본체
├── scripts/              # jarvis.py 편의 스크립트 및 빌드(.bat) 파일 보관
└── packaging/            # PyInstaller Spec 및 런타임 DLL 훅 파일들
```

사내 문서 규칙이 개정되는 등 PDF 파일 내용이 변경되었을 때, 아래 절차에 따라 최신 데이터를 반영할 수 있습니다.
1. `data\documents` 폴더에 갱신할 PDF 파일들을 업로드합니다.
2. `venv\Scripts\python.exe src\rag\ingest.py` 실행 (기존 데이터를 초기화하고 새로운 벡터를 생성하는 과정)
3. `venv\Scripts\python.exe jarvis.py` 를 실행하여 대화를 통해 변경 사항이 정상적으로 반영되었는지 확인합니다.

---

## 📦 5. 최적화 및 배포 관리 (Deployment)

### 패키징(PyInstaller) 및 실행 파일(.exe) 빌드
현재 작동이 확인된 소스코드와 최신 RAG 벡터 데이터베이스, 무거운 내부 에셋 모범 파일들을 모조리 모아 단 한 방에 `.exe`로 압축합니다.

```powershell
scripts\build_jarvis.bat
```
빌드가 성공적으로 끝나면 `dist\jarvis\jarvis.exe` 가 단일로 생성되며, 최종 배포를 위해 `jarvis_package.zip`이라는 이름의 묶음이 프로젝트 최상단에 등장합니다!

### 캐시 최적화 및 빌드 속도를 높이는 꿀팁
앱 빌드 용량의 대부분(약 450MB)은 `data\model_cache`에 저장되는 오프라인 텍스트 임베딩 언어 모델 뭉치입니다. 인터넷 환경과 무관한 완벽한 100% 로컬 구동을 위해 기본 파이프라인에 이 캐시를 포함시켰습니다.

- `build` 폴더는 용량이 크더라도 유지하는 것을 권장합니다. (PyInstaller 분석 캐시를 활용하면 재빌드 시간을 대폭 단축할 수 있습니다).
- `data\hf_cache`는 임시 파일이므로 배포 시 제외해도 무방합니다.
- 윈도우 디펜더 등 보안 프로그램이 `dist`나 `build` 폴더를 실시간 검사할 경우 빌드 속도가 현저히 느려지므로, 해당 폴더를 검사 예외로 등록하는 것을 권장합니다.

---

## ❓ 6. 트러블슈팅 및 운영 (Troubleshooting)

### Q. Ollama가 아예 응답하지 않고 무한 대기합니다.
- 설치 여부와 `ollama pull [모델명]`을 터미널에서 올바르게 타이핑했는지 확인하세요.
- `.env` 파일 혹은 코드 내의 `OLLAMA_MODEL_NAME`과 실제 PC에 다운로드한 모델명이 매칭되는지 체크하세요.

### Q. '휴가 규정'을 물어봐도 딴소리를 합니다. (내부 문서 RAG 실패)
- `data\documents` 폴더 안에 실제로 PDF 문서가 존재하는지 눈으로 확인하세요.
- PDF를 수정한 이후, `src\rag\ingest.py` 스크립트를 실수로 까먹고 안 돌려서 과거 구식 벡터를 계속 참조하고 있을 확률이 제일 높습니다.

### Q. 'PC 파일 검색'을 시켰는데 프로그램 작동이 잘 안 됩니다.
- `assets\everything\Everything.exe` 본체가 해당 폴더에 제대로 있는지 파악하세요.
- 포트 충돌(`EVERYTHING_PORT`)이 발생하는지 `.env` 세팅을 점검하세요.

### Q. 실행 파일 빌드가 비정상적으로 종료되거나 오류가 발생합니다.
- 생성된 `.exe` 파일이나 `dist` 경로의 파일이 실행 중이거나 다른 프로세스에 의해 점유(Lock)되어 있는지 확인하세요.

### Q. 답변 타이핑 속도가 느리거나 저사양 PC에서 화면이 끊깁니다. (성능 최적화 완료)
- **증상**: AI가 답변을 생성할 때 화면이 뚝뚝 끊기거나, 답변이 다 나왔는데도 타이핑 애니메이션이 늦게 따라오는 현상.
- **기술적 해결**:
    - **비동기 마크다운 렌더링**: 무거운 마크다운 → HTML 변환 작업을 UI 스레드가 아닌 **백그라운드 스레드풀(`QThreadPool`)**에서 처리하여 UI 프리징을 완벽히 제거했습니다.
    - **유기적 타이핑 버퍼**: `ChatStreamState`를 통해 LLM 생성 속도와 화면 출력 속도를 분리하고, 버퍼 크기에 따라 타이핑 속도를 자동 조절(`Adaptive Speed`)합니다.
    - **렌더링 스로틀링**: 매 글자마다 화면을 다시 그리지 않고 최적의 주기로 성능을 안배하여 사양에 관계없이 부드러운 프레임을 유지합니다.
    - **즉시 노출(Flush on Finish)**: AI가 답변 생성을 완료하는 즉시 남은 타이핑을 생략하고 전체 내용을 노출하여 대기 시간을 최소화했습니다.
195: 
196: ### Q. 최근 유행하는 LangChain 같은 프레임워크를 왜 사용하지 않았나요?
197: - **응답 대기 시간 최소화**: LLM이 질문의 의도를 분석하기 위해 '생각'하는 단계를 최대한 생략하고 즉각 답변하기 위해, 직접 구현한 정규식 기반 라이트웨이트 라우팅을 채택했습니다.
198: - **빌드 및 배포 효율화**: 특정 기능에 최적화된 최소한의 라이브러리만 사용하여 PyInstaller 빌드 시간을 단축하고, 실행 파일(`.exe`) 최종 용량을 가볍게 유지하는 데 중점을 두었습니다.
199: - **유지보수와 제어권**: 프레임워크의 추상화 계층 없이 코드와 프롬프트 흐름을 직접 제어하므로, 비서 페르소나와 답변 일관성을 더 세밀하게 관리할 수 있습니다.

---
**💡 [권장 문서 교체 및 운영 프로세스 순서도]**
`1. PDF 버전 반영` ➡️ `2. ingest.py 벡터 스크립트 실행` ➡️ `3. 로컬 jarvis.py로 테스트 발송` ➡️ `4. 이상 발견 없을 시 build_jarvis.bat 가동` ➡️ `5. 완성된 zip을 사내 최종 배포`

---

## 🤖 7. 부록: Vibe Coding 개발기 (AI 에이전트 협업 실험)

이 프로젝트는 단순히 코드를 짜는 것을 넘어, **"Vibe Coding"** — 즉, 프롬프트와 AI 에이전트 간의 상호작용만으로 복잡한 결과물을 만들어내는 실험적 프로세스로 구축되었습니다. 여러 LLM과 AI 기반 IDE/Agent를 조합하여 실제 엔터프라이즈 환경의 도구를 어디까지 자동화할 수 있는지 탐구한 기록입니다.

### 🧠 LLM별 협업 워크로드 분석

각 모델의 특성에 맞춰 역할을 분담하여 개발 효율을 극대화했습니다.

| 모델 | 주요 역할 | 사용 환경 | 한 줄 요약 |
| :--- | :--- | :--- | :--- |
| **Claude** | **아키텍처 및 설계** | 웹(Web), VSCode, Google Antigravity | "설계 감각이 탁월한 시니어 개발자" — 복잡한 리팩토링과 구조 정의에 강점 |
| **GPT (4/5)** | **구현 및 디버깅** | 웹(Web), VSCode, Google Antigravity | "실무에 강한 올라운더" — 전반적인 코드 생성 및 도구 연동, 에러 수정 편의성 |
| **Codex** | **코드 로직 구현** | VSCode, Google Antigravity | "강렬한 실무형 엔진" — 군더더기 없는 논리와 압도적인 코드 구현력으로 핵심 실무 로직을 완벽하게 관통 |
| **Gemini** | **리서치 및 자료조사** | 웹(Web), Google Antigravity | "박학다식한 보조역" — 방대한 문서 분석 및 외부 레퍼런스 수집에 특화 |

### 🛠️ Agent 및 개발 환경 (IDE)

*   **VSCode + Extensions**: 프로젝트의 최종 통제소이자 디버깅 및 패키징을 위한 핵심 환경입니다.
*   **Cline (VSCode Plugin)**: 단순 반복 작업이나 파일 입출력 자동화 등 보조적인 개발 루프를 지원하는 용도로 활용되었습니다.
*   **Antigravity (Agent IDE)**: Google Antigravity 플러그인 및 에이전트 기반의 자동화 환경을 통해 빠른 프로토타이핑과 프로젝트 단위의 코드 제어를 수행했습니다.

### ⚙️ Vibe Coding 핵심 인사이트

1.  **적재적소의 모델 배치**: 단일 모델로는 한계가 명확합니다. `Claude(설계)` ➡️ `Codex(핵심 구현)` ➡️ `GPT(디버깅)` 순의 유기적인 협업 구조가 가장 효율적이었습니다. 특히 Codex는 실무 로직의 완성도 면에서 독보적인 성능을 보여주었습니다.
2.  **에이전트는 '지렛대'**: 에이전트는 개발자를 대체하는 것이 아니라, 개발 루프를 비약적으로 압축하는 도구입니다.
3.  **컨텍스트 관리가 성패를 결정**: 프로젝트 규모가 커질수록 모델이 상태를 잃지 않도록 작업 단위를 쪼개고 명확한 피드백 루프를 유지하는 것이 가장 큰 병목이자 해결 과제였습니다.

### 🧩 최종 결론

> **"AI는 개발의 가속 페달이지만, 핸들은 여전히 사람이 잡아야 합니다."**

이 프로젝트는 AI 에이전트 기반 개발 방식의 **강력함**과 이를 정교하게 통제하는 것의 **어려움**을 동시에 보여주는 결과물입니다. 구조적인 통제 하에 사용될 때, AI는 상상만 하던 아이디어를 현실로 만드는 가장 빠른 지름길이 됩니다.

---

### 🚀 추천 개발 파이프라인 (Experience-based)

> **`Claude (설계/구조)`** ➡️ **`Codex (핵심 로직 구현)`** ➡️ **`GPT (검증 및 디버깅)`** ➡️ **`VSCode / Google Antigravity (최종 제어)`**

*※ Cline은 단순 반복 작업 및 파일 입출력 자동화 시 보조적으로 활용했습니다.*

---

